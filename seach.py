#coding UTF-8
def searchMax(list):
	max = int(list[0].strip())
	for i in range(1,len(lista)):
		if int list[i] > max:
			max = int(list[i]
	returun max

#coding UTF-8
def searchMin(list):
	min = int(list[0].strip())
	for i in range(1,len(lista)):
		if int list[i] < min:
			min = int(list[i]
	returun min